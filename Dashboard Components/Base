import streamlit as st
from streamlit_extras import add_vertical_space

# Page Config
st.set_page_config(page_title="Internet Usage Analysis", layout="wide")

# Title
st.markdown("# 🌐 INTERNET USAGE ANALYSIS", unsafe_allow_html=True)

# Layout
col1, col2, col3 = st.columns([1, 0.2, 1])

# Tabs as Buttons
with col1:
    if st.button("🌍 CONTINENT TAB", use_container_width=True):
        st.session_state['tab'] = 'continent'
with col3:
    if st.button("🌏 COUNTRY TAB", use_container_width=True):
        st.session_state['tab'] = 'country'

# Social Media Icons
with col2:
    st.markdown("""
    <div style="display: flex; justify-content: center; gap: 10px;">
        <a href="https://www.linkedin.com" target="_blank">
            <img src="https://cdn-icons-png.flaticon.com/512/174/174857.png" width="30">
        </a>
        <a href="https://github.com" target="_blank">
            <img src="https://cdn-icons-png.flaticon.com/512/25/25231.png" width="30">
        </a>
    </div>
    """, unsafe_allow_html=True)

# Content Display
st.markdown("## 📊 Analysis of Graph 1")
st.write("Here, we analyze internet growth in different continents.")

# Placeholder for Graph
st.markdown("### **Graph 1: Internet Growth**")
st.line_chart([3, 2, 5, 7])
